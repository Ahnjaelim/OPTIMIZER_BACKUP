<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wellconn.optimizer.mapper.LightHouseMapper">

	<select id="selectNow" resultType="String">
		select now()
	</select>
	
	<insert id="insertLightHouse" parameterType="LightHouseVO" useGeneratedKeys="true" keyProperty="lh_sn">
		INSERT INTO public.om_lighthouse_log
		(site_no, site_address, page_no, lh_json, lh_type, lh_score, rgstr_dt)
		VALUES(#{site_no}, #{site_address}, #{page_no}, #{lh_json}, #{lh_type}, #{lh_score}, now());
	</insert>
	
	<select id="selectLightHouse" parameterType="LightHouseVO" resultType="LightHouseVO">
		SELECT * FROM om_lighthouse_log oll 
		WHERE page_url = #{page_url}
			AND lh_type = #{lh_type}
		ORDER BY rgstr_dt DESC  
		LIMIT 1
	</select>
	
</mapper>